export enum FORM_ELEMENTS {
  TEXT = "text",
  RADIO = "radio",
  CHECKBOX = "checkbox",
  DROPDOWN = "dropdown",
  FILE_UPLOAD = "file-upload",
}

export enum THEME {
  DARK = "dark",
  LIGHT = "light",
}
